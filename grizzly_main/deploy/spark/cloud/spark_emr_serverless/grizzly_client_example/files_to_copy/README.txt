These files are meant to be executed from the client repository, not from grizzly.

Thus, you should expect to see errors within those files for some imports. For example, the main config will be imported
from the directory: spark_emr_serverless_example/ (which is the folder from the root path of the client repository
where we will place all the files required to run the example)